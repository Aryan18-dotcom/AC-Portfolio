export default {
  theme: {
    extend: {
      fontFamily: {
        primary: 'var(--font-primary)',
        secondary: 'var(--font-secondary)',
        tertairy: 'var(--font-tertairy)',
        quaternary: 'var(--font-quaternary)',
      }
    }
  }
}